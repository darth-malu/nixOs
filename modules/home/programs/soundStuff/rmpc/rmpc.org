#+title: Rmpc configuration
#+auto_tangle: t
#+startup: content indent

* Start
#+begin_src nix :tangle rmpc-config.nix
''
  (
#+end_src
* Keybinds
** starter
#+begin_src nix :tangle rmpc-config.nix
keybinds: (
#+end_src

** global
#+begin_src nix :tangle rmpc-config.nix
global: {
  ":":       CommandMode,
  ",":       VolumeDown,
  "s":       Stop,
  ".":       VolumeUp,
  "<Tab>":   NextTab,
  "<S-Tab>": PreviousTab,
  "1":       SwitchToTab("Playing"),
  "4":       SwitchToTab("Dir"),
  "3":       SwitchToTab("Lists"),
  "2":       SwitchToTab("Find"),
  "q":       Quit,
  ">":       NextTrack,
  "p":       TogglePause,
  "<":       PreviousTrack,
  "f":       SeekForward,
  "z":       ToggleRepeat,
  "x":       ToggleRandom,
  "c":       ToggleConsume,
  "v":       ToggleSingle,
  "b":       SeekBack,
  "~":       ShowHelp,
  "I":       ShowCurrentSongInfo,
  "O":       ShowOutputs,
  "P":       ShowDecoders,
},
#+end_src

** navigation
#+begin_src nix :tangle rmpc-config.nix
navigation: {
  "k":         Up,
  "j":         Down,
  "h":         Left,
  "l":         Right,
  "<Up>":      Up,
  "<Down>":    Down,
  "<Left>":    Left,
  "<Right>":   Right,
  "<C-k>":     PaneUp,
  "<C-j>":     PaneDown,
  "<C-h>":     PaneLeft,
  "<C-l>":     PaneRight,
  "<C-u>":     UpHalf,
  "N":         PreviousResult,
  "a":         Add,
  "A":         AddAll,
  "r":         Rename,
  "n":         NextResult,
  "g":         Top,
  "<Space>":   Select,
  "<C-Space>": InvertSelection,
  "G":         Bottom,
  "<CR>":      Confirm,
  "i":         FocusInput,
  "J":         MoveDown,
  "<C-d>":     DownHalf,
  "/":         EnterSearch,
  "<C-c>":     Close,
  "<Esc>":     Close,
  "K":         MoveUp,
  "D":         Delete,
},
#+end_src

** Queue
#+begin_src nix :tangle rmpc-config.nix
queue: {
  "D":       DeleteAll,
  "<CR>":    Play,
  "<C-s>":   Save,
  "a":       AddToPlaylist,
  "d":       Delete,
  "i":       ShowInfo,
  "C":       JumpToCurrent,
},
#+end_src

** ender
#+begin_src nix :tangle rmpc-config.nix
),
#+end_src

* Misc
#+begin_src nix :tangle rmpc-config.nix
  address: "127.0.0.1:6600",
  password: None,
  theme: None,
  cache_dir: "~/Music/rmpc/cache",
  lyrics_dir: "~/Music/rmpc/lyrics",
  on_song_change: "songart",
  volume_step: 5,
  scrolloff: 5,
  wrap_navigation: true,
  enable_mouse: true,
  enable_config_hot_reload: true,
  status_update_interval_ms: 1000,
  select_current_song_on_change: false,
  browser_song_sort: [Disc, Track, Artist, Title],
#+end_src

** Progress bar
#+begin_src nix :tangle rmpc-config.nix
  progressbar_look: `["[", "─", "⊙", "┄", "]"],
#+end_src

** select current song on change
#+begin_src nix :tangle rmpc-config.nix
  select_current_song_on_change: false,
#+end_src

** max fps
#+begin_src nix :tangle rmpc-config.nix
  max_fps: 30,
#+end_src

progress_bar: (symbols: [start, elapsed, thumb, track, end]

* cava
#+begin_src nix :tangle rmpc-config.nix
cava: (
  framerate: 60, // default 60
    autosens: true, // default true
      sensitivity: 100, // default 100
        lower_cutoff_freq: 50, // not passed to cava if not provided
          higher_cutoff_freq: 10000, // not passed to cava if not provided
            input: (
              method: Fifo,
                source: "/tmp/mpd.fifo",
                sample_rate: 44100,
                channels: 2,
                sample_bits: 16,
            ),
            smoothing: (
              noise_reduction: 77, // default 77
                monstercat: false, // default false
                  waves: false, // default false
            ),
            // this is a list of floating point numbers thats directly passed to cava
            // they are passed in order that they are defined
            eq: []
),
#+end_src

* layout
#+begin_src nix :tangle rmpc-config.nix
layout: Split(
  direction: Vertical,
    panes: [
      (
        size: "8",
        pane: Split(
          direction: Horizontal,
            panes: [
              (
                size: "21",
                pane: Pane(AlbumArt),
              ),
              (
                size: "100%",
                pane: Split(
                  direction: Vertical,
                    panes: [
                      (
                        size: "5",
                        pane: Pane(Header),
                      ),
                      (
                        size: "1",
                        pane: Pane(ProgressBar),
                      ),
                      (
                        size: "2",
                        pane: Pane(Tabs),
                      ),
                    ]
                )
              ),
            ]
        ),
      ),
      (
        size: "100%",
        pane: Pane(TabContent),
      ),
    ],
),
#+end_src

* Endo
#+begin_src nix :tangle rmpc-config.nix
)''
#+end_src
